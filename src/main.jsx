import React from "react";
import ReactDOM from "react-dom/client";
import CampoutPermissionForm from "./CampoutPermissionForm";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CampoutPermissionForm />
  </React.StrictMode>
);
